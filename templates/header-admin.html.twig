<header class="p-3 mb-3 border-bottom">
	<div class="container">

		<div class="d-flex flex-wrap align-items-center justify-content-center justify-content-lg-start">
			<a href="{{path('accueil')}}" class="d-flex align-items-center mb-2 mb-lg-0 text-dark text-decoration-none">
				<img src="/assets/images/logo.png">
			</a>

			<ul class="nav col-12 col-lg-auto me-lg-auto mb-2 justify-content-center mb-md-0">
				<li>
					<a href="{{path('admin_produit')}}" class="nav-link px-2 link-secondary">Produits</a>
				</li>
				<li>
					<a href="{{path('admin_slide')}}" class="nav-link px-2 link-dark">Slides</a>
				</li>
				<li>
					<a href="{{path('admin_categorie')}}" class="nav-link px-2 link-dark">Catégories</a>
				</li>
			</ul>

			<a class="btn btn-primary" href="{{path('app_logout')}}">Sign out</a>


		</div>
	</div>
</header>
