{% extends 'base.html.twig' %}

{% block title %}Administration
{% endblock %}

{% block body %}
	<table class="table">
		<thead>
			<tr>
				<th>ID</th>
				<th>Image</th>
				<th>Désignation</th>
				<th>Description</th>
				<th>Prix</th>
				<th>Actions</th>
			</tr>
		</thead>
		<tbody>
			{% for produit in listeProduit %}
				<tr>
					<td>{{produit.id}}</td>
					<td><img style="width:50px;height:50px" src="/uploads/{{produit.nomImage}}"></td>
					<td>{{produit.designation}}</td>
					<td>{{produit.description}}</td>
					<td>{{produit.prix}}</td>
					<td>
						<a class="btn badge bg-success">Modifier</a>
						<a class="btn badge bg-danger" href="{{path ('suppression_produit', {id:produit.id})}}">Supprimer</a>
					</td>
				</tr>
			{% endfor %}

		</tbody>
	</table>
{% endblock %}
